# 更新日志

## 1.2.3（2026-09-19）

### API 102 迁移

- libxposed 编译与测试依赖从 `101.0.1` 升级到 Maven Central 正式版 `102.0.0`。
- 模块声明更新为 `minApiVersion=102`、`targetApiVersion=102`，需要支持 API 102 或更高版本的 Xposed 框架；API 101 设备保留使用 1.2.2。
- 显式设置 `autoHotReload=false`，保留重启应用后加载新模块的方式，不改变现有 Hook 注册与应用生命周期行为。
- 生命周期测试中的模拟框架入口适配 API 102 的双参数 `attachFramework`；生产代码不调用这一框架内部入口。
- 保留微信 8.0.78（3180）的定位适配及其他应用逻辑，版本提升为 1.2.3 / versionCode 15。

### 发布与验证边界

- 提供已签名、不可调试的 Release APK，沿用既有签名证书以支持覆盖安装。
- 先进行离线测试、Lint、构建及 APK 元数据检查，再同步代码和发布安装包。
- **2026-09-20 补充真机验收通过**：OPPO PME110 / Android 17 / API 37，LSPosed IT 2.2.0-it（7881），微信 8.0.78（3180）。工具实测 Release 覆盖安装、API 102 加载、微信两个 Hook 安装与冷启动保留登录；用户确认双端在线、新消息及重启均正常。重启结果来自用户确认，本轮未再次执行整机重启。
- 正式发布沿用已安装验证的 v1.2.3 / 15 APK，包哈希、签名及源码标签不变；仅补充验收记录和发布说明，不重新打包。
- 飞书双端并发登录仍未通过，相关实验限制不变。

## 1.2.2（2026-09-19）

### 微信适配

- 适配微信 8.0.78（3180）拆分后的折叠屏判断：保留旧字符串定位，增加按辅助方法调用关系定位的回退规则。改动来自上游 [PR #51](https://github.com/Houvven/I-Am-Pad/pull/51)。
- 保留 1.2.1 的 Application.attach 生命周期修复，避免回退已验证的启动行为。

### 验证与交付

- 对官网 arm64 APK 完成版本、签名和方法唯一性核验。
- 适配代码已在 OPPO PME110 / Android 16 / 微信 8.0.78（3180）完成扫码登录、应用冷启动、整机重启验证；用户确认重启后 iPhone 与安卓均在线并能收到新消息。该轮完整验证使用候选 Debug 构建，详见 [实测记录](docs/wechat-8.0.78.md)。
- 正式安装包使用 Release 构建，关闭 debuggable，启用 R8 与资源压缩；沿用 1.2.1 签名证书以支持保数据覆盖安装。
- GitHub CI 使用项目要求的 JDK 21，执行检查并仅构建 Release；CI 产物为未签名验证包，已签名安装包在 GitHub Releases 提供。

### 已知限制

- 本轮未重新验收其他微信版本、小红书、QQ 或飞书；旧版微信仍保留原定位规则。
- 飞书仍为实验适配，不宣称解决双端并发登录限制。

## 1.2.1（2026-09-11）

### 修复

- 修复应用启动 Hook 替换原始 `Application` 生命周期的问题：现在先执行系统原方法，再安装应用 Hook，保留原返回值和异常。
- 移除 QQ 自动删除 MMKV 缓存并终止进程的逻辑，避免破坏账号与设备状态。
- 增加启动与 Hook 命中日志，便于判断模块是否在系统重启后正常加载。

### 适配

- 小红书继续使用服务端识别所需的 Pad 机型和本地设备类型。用户已在 Android 16 真机验证：系统重启后账号保持在线并能收消息。
- 微信在 Android 16 真机验证：系统重启后账号保持在线并能收消息。
- 增加飞书 7.76.14 实验适配：模拟 Xiaomi Pad 6 型号 `23043RP34G`、tablet 特征及 Passport 设备型号。

### 已知限制

- 飞书登录确认页能够显示模拟机型，但手机与模拟 Pad 同时在线未通过；本版本不宣称已解决飞书多端登录限制。
- 用户随后确认真实鸿蒙平板也会挤掉同一 iPhone，说明当前缺少可用于判定插件成败的真实 Pad 共存对照。
- QQ 尚未完成系统重启后的同口径真机验收。

### 验证环境

- Android 16 / API 36
- LSPosed IT 2.2.0-it (7881)
- 微信 8.0.77、小红书 9.46.0、QQ 9.3.60、飞书 7.76.14
